version: "3"

services:
  
  api1:
    container_name: api1
    build: ./api

    environment:
      - MESSAGE=${NOMBRE}
    ports:
      - "3000:3000"

  api2:
    container_name: api2
    build: ./api

    environment:
      - MESSAGE=${NOMBRE_2}
    ports:
      - "3002:3000"

  